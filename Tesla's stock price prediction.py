# -*- coding: utf-8 -*-
"""Untitled0.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1ryhtoqy2oAFHmveRazQcVdmG0_jtLD1t

# This program predicts the closing stock price of a corporation using the past 60 day stock price.
"""

import math
import pandas_datareader as web
import numpy as np
import pandas as pd
from sklearn.preprocessing import MinMaxScaler
from keras.models import Sequential
from keras.layers import Dense, LSTM
import matplotlib.pyplot as plt
plt.style.use('fivethirtyeight')

#Getting the stock quote of Tesla from January 1st, 2012 to December 17th, 2019
df = web.DataReader('TSLA', data_source='yahoo', start='2012-01-01', end='2019-12-17') 
#Show the data 
df

#Data size 
df.shape

#Graph showing the closing price history
plt.figure(figsize=(16,8))
plt.title('Close Price History')
plt.plot(df['Close'])
plt.xlabel('Date',fontsize=18)
plt.ylabel('Close Price USD ($)',fontsize=18)
plt.show()

#Creating a new dataframe using the 'Close' column
data = df.filter(['Close'])

dataset = data.values

#Calculating the number of rows top train the model
training_data_len = math.ceil( len(dataset) *.8)

#Scaling the data to be values between 0 and 1 
scaler = MinMaxScaler(feature_range=(0, 1)) 
scaled_data = scaler.fit_transform(dataset)

#Creating the training data set
train_data = scaled_data[0:training_data_len  , : ]

#Data Split
x_train=[]
y_train = []
for i in range(60,len(train_data)):
    x_train.append(train_data[i-60:i,0])
    y_train.append(train_data[i,0])

#Converting splitted data to numpy arrays
x_train, y_train = np.array(x_train), np.array(y_train)

#Reshaping the data into the shape accepted by the LSTM
x_train = np.reshape(x_train, (x_train.shape[0],x_train.shape[1],1))

#Building the LSTM network model
model = Sequential()
model.add(LSTM(units=50, return_sequences=True,input_shape=(x_train.shape[1],1)))
model.add(LSTM(units=50, return_sequences=False))
model.add(Dense(units=25))
model.add(Dense(units=1))

#Compiling the model
model.compile(optimizer='adam', loss='mean_squared_error')

#Train the model
model.fit(x_train, y_train, batch_size=1, epochs=1)

#Creating a test data set
test_data = scaled_data[training_data_len - 60: , : ]

#Creating the x_test and y_test data sets
x_test = []
y_test =  dataset[training_data_len : , : ]
for i in range(60,len(test_data)):
    x_test.append(test_data[i-60:i,0])

#x_test to a numpy array 
x_test = np.array(x_test)

#Reshaping the data to what LSTM accepts
x_test = np.reshape(x_test, (x_test.shape[0],x_test.shape[1],1))

#Getting the models predicted price values
predictions = model.predict(x_test) 
predictions = scaler.inverse_transform(predictions)#Undo scaling

#Calculating RMSE
rmse=np.sqrt(np.mean(((predictions- y_test)**2)))
rmse